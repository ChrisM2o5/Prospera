// @ts-ignore Dependencies are resolved when the Vite config is executed.
import { defineConfig } from "vite";
// @ts-ignore Dependencies are resolved when the Vite config is executed.
import react from "@vitejs/plugin-react";

export default defineConfig({
plugins: [react()],
});